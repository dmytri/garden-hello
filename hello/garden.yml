kind: Module
type: container
name: hello
description: hello sayer
services:
  - name: hello
    ports:
      - name: http
        containerPort: 8080
        servicePort: 80
    ingresses:
      - path: /
        port: http
        hostname: localhost
