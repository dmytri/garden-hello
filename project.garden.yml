kind: Project
name: cabinet

environments:
  - name: local
    variables:
      base-hostname: cabinet.192.168.50.35.nip.io

providers:
  - name: local-kubernetes
    environments: [local]
    context: minikube

defaultEnvironment: "local"

